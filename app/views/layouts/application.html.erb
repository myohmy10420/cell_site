<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width,height=device-height,initial-scale=1.0"/>
    <title>就是便宜</title>
    <meta name="description" content="快速找手機來這就是便宜">
    <meta name="keywords" content="js,jspe,就是便宜,手機便宜,手機就是便宜" />
    <%= favicon_link_tag "favicon.jpg" %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <%= render partial: "modules/header" %>
    <%= render partial: "modules/flash" %>
    <div class="wrapper">
      <div class="wrapper-left">
        <%= render partial: "modules/side_bar" %>
      </div>
      <div class="wrapper-body">
        <%= yield %>
      </div>
      <div class="wrapper-right">
        <%= render partial: "modules/side_bar_ads" %>
      </div>
    </div>
    <%= render partial: "modules/footer" %>
  </body>
</html>
