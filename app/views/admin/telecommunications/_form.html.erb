<%= simple_form_for @telecommunication, url: url do |f| %>
  <%= f.input :name, label: "名稱" %>
  <%= image_tag @telecommunication.logo.url(:normal) if @telecommunication.logo.present? %>
  <%= f.input :logo, label: "圖片", as: :file %>
  <%= f.button :submit, "提交", class: "btn btn-primary" %>
<% end %>
