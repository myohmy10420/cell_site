<div class="stores">
  <section>
    <div class="square square--red"></div>
    <span class="font font--grey">門市據點</span>
  </section>

  <section>
    <% @stores.each do |store| %>
      <%= link_to "##{ store.name }", class: "btn-link" do %>
        <div class="btn btn--red btn--group"><%= store.name %></div>
      <% end %>
    <% end %>
  </section>

  <% @stores.each do |store| %>
    <div class="info_box" id="<%= store.name %>">
      <div class="info_box-image">
        <%= image_tag store.image.url(:medium) %>
      </div>

      <div class="infos">
        <h4 class="font font--red"><%= store.name %></h4>
        <p>
          <span class="font font--red font--400w">服務電話:</span>
          <span class="font font--200w">
            <%= render_dash_if_empty(store.service_line) %>
          </span>
        </p>
        <p>
          <span class="font font--red font--400w">傳真專線:</span>
          <span class="font font--200w">
            <%= render_dash_if_empty(store.fax) %>
          </span>
        </p>
        <p>
          <span class="font font--red font--400w">行動電話:</span>
          <span class="font font--200w">
            <%= render_dash_if_empty(store.phone) %>
          </span>
        </p>
        <p>
          <span class="font font--red font--400w">E-mail:</span>
          <span class="font font--200w">
            <%= render_dash_if_empty(store.email) %>
          </span>
        </p>
        <p>
          <span class="font font--red font--400w">Line ID:</span>
          <span class="font font--200w">
            <%= render_dash_if_empty(store.line_ID) %>
          </span>
        </p>
        <p>
          <span class="font font--red font--400w">門市地址:</span>
          <span class="font font--200w">
            <%= render_dash_if_empty(store.address) %>
          </span>
        </p>
        <p>
          <span class="font font--red font--400w">營業時間:</span>
          <span class="font font--200w"><%= store.time %></span>
        </p>
        <%= link_to store.google_map_url, target: "_blank" do %>
          <div class="btn btn--red">瀏覽地圖</div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
